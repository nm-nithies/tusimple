The maximum difference (max_diff) originates from the LayerNorm operation. These small differences accumulate and become significant due to subsequent MatMul operations. MatMul amplifies the numerical discrepancies, leading to larger overall differences.
